<!DOCTYPE html>
<html>
<head>
  <title>PRV - {{settings.github.user}}/{{settings.github.repo}}</title>
  <link rel='stylesheet' href='/stylesheets/style.css' />
</head>
<body>

<div class="navbar navbar-inverse">
  <div class="navbar-inner">
    <div class="container">
      <a class="brand" href="/">
        Pull Request Viewer -
        {{settings.github.user}}/{{settings.github.repo}}
      </a>
      <ul class="nav pull-right">
        <li>
          <img src="{{profile._json.avatar_url}}"/>
          {{profile.displayName}}
        </li>
      </ul>
    </div>
  </div>
</div>

<div class="container">

  <table class="table table-bordered table-striped">

    <thead>
    <tr>
      <th>Number</th>
      <th>Title</th>
      <th>Branch</th>
      <th>Submitter</th>
      <th>Reviewer</th>
      <th>Comments</th>
      <th>Status</th>
      <th>Last Update</th>
    </tr>
    </thead>

    <tbody>
    {{#pulls}}
    <tr class="pullrequest {{class}}">
      <td><a href="{{html_url}}">#{{number}}</a></td>
      <td>{{title}}</td>
      <td><code>{{head.ref}}</code></td>
      <td>
        <img src="{{user.avatar_url}}"/>
        {{user.login}}
      </td>
      <td>{{assignee}}</td>
      <td>{{num_comments}}</td>
      <td>{{status}}</td>
      <td>{{last_update}}</td>
    </tr>
    {{/pulls}}
    </tbody>

  </table>

</div>

</body>
</html>
